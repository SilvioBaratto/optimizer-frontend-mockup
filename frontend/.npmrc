# @angular/build 21.x declares peerOptional vitest ^4, but the project is pinned
# to vitest ^3.2 for the @angular/build:unit-test runner. Without this, npm ci
# fails with ERESOLVE locally, in CI, and on Vercel's install step.
legacy-peer-deps=true
